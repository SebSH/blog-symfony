{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commentary</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentary.id }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ commentary.pseudo }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ commentary.content }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{% if commentary.createdAt %}{{ commentary.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Modifiedat</th>
                <td>{% if commentary.modifiedAt %}{{ commentary.modifiedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commentary_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('commentary_edit', { 'id': commentary.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
